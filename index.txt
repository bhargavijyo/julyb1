Html stands for hyper text markup language - 
it is used to create web page and structure content .

It is not programming language 
It's markup language 
It tells the browser that the document is an HTML5 

//CSS 
css is a cascading style sheet 
it is used to style and design webpage 
change text ,color, background 
animation and transition too

//Task -1 
inline -- For styling single element 
internal -- for styling html page 
written inside the <style> 
tag in the <header> secvtion of the Html file.
external -- attached with another file for proper ui and design
selector{
    property:value;
}


//Task-2 
selectors -- A css selector used to select html elements so that we can apply styles to them
1.Element selector --  It selects all html elements with the same tag name (h1, p, button)
2.class selector -- Class selector is used to style one or more html elements(.)
3.id selector -- Id selector styles  only one unique html element and id should be used only once on a page (#)
4.universal selector -- Selects every html element on the web page(*)
5.group selector --  This will used to apply same style to multiple or different elements


//colors and background properties : 
used to change the text color 
selector{
    color:value;
}
color -- name, RGB, HEXDECIMAL WAY
color:orange;
color:rgb(255,0,0);
color:#ffffff

background properties: Used to change the background of the web page
background-img: url("");
background-repeat:repeat;(control  the web page)
    repeat : background img repeats again nd again  like horizontally and vertically and fills entire page
    no-repeat :
    It displays only once

background-size: cover/contain;
background-position:center/top/bottom/left/right;

//Text styling  -- It is used to change appearence of text on a webpage 
1.font-family:Times new roman, serief (used to change the font style)
2.font-size : 80px ,20px, or 50% ;(used to change the size of the font)
3.text-align : center,left,right(we can align text based on user need)
4. text-transform : it can used to convert text (uppercase, lowercase)
5.text-decoration :underline, overline, line-through
6.font-weight : bolder;
7.line-height : changes the space between lines
8.letter-spacing : space between letters;

//Box Modeling -- In this every Html element is treated as box and 
it determines how much space each element occupies on web page.
1.content -- Actual data inside the tags like text,img,buttons etc.
2.padding -- It is the space between the content and border.
3.border -- Is the line surrounding padding and content.
4.margin --  iSs the space outside the border.

//CSS positions -- used to control positon of an element on web page(how it placed like that)
1.position:static;  (default )
2.relative  -- original place(u can move it using top, left, right)
3.absolute -- the element removed from original position 
                absolute can search the non-static parent, once it reach the non-static parent it will
                act as the same as relative .
4.fixed -- Fixed place no change
5.sticky -- using top:0; once it reach top it will act as same fixed

//CSS display Properties : 
    used to show how an html element is displayed and how
    it behaves in the layout 
    syntax:
        selector{
            display : value;
        }
    1.display:block ;-- 
        It contains block-level element starts on a new line and
        normally takes the full available width 
        block elements :<div>,<p>,<h1>,<section>...etc
    
    2. display : inline; --Inline element does not start on new line 
                            It takes only required width by it's content 
                            inline elements -- <span>,<a>,<strong>,<em>
                            Box1 Box2 Box3

    3.display : inline-block; -- combination of inline + block 
                                stays in the same line like inline 
                                allows width and height like block 

    4.display: none; -- completely removes the element from the page layout 
                        The element not visible and it doesn't occupy any space.
    
    5.display : flex; -- It used to create a flexbox layout,
                        that makes easy to align, distribute ,size elements inside container. 

                        It is one-dim layout 
                        column/row only 
                        best small components
                        ex:Navbar,cards

                        It's useful for arranging elements in a row or column
                        display : flex;
                        flex-direction : row, row-reverse, col,col-reverse;
                        justify-content//align-content : center,top, end, space-around;
                        align-items : start,top,end,center;
                        gap: it can be set by user (10px,20px;)
                        flex-wrap: nowrap, wrap,wrap-reverse;(Allows items to move to the next line.)
                        flex on item: flex,1,2,3
                        order: we can change the visual order.

   6.grid -- 2d layout 
            row and column 
            it is best for full page creation 
            ex:full page, section 
            display:grid; 

            grid-template-columns : --used to define in columns 

                grid-template-columns : 200px,300px,400px;
                                      : 30%,40%,30%;

                                      1 2

                                      2 2

                                      2 2
                                    
                                    

